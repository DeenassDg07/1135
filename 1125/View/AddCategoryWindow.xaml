<Window x:Class="_1125.View.AddCategoryWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125.View"
        mc:Ignorable="d"
          Background="#F5F5F5" 
        Title="CategoryWindow" Height="420" Width="500">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0"   Height="25" Width="200"  Background="White" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0" />

        <ScrollViewer Grid.Column="0" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ListBox Background="#F5F5F5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Button  Height="25" Width="200" FontFamily="Times New Roman"  Background="#F5F5F5" BorderBrush="Transparent"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Название категории:"  FontSize="15" FontFamily="Times New Roman"   />
            <TextBox Height="25" Width="200"/>
        </StackPanel>

        <Button Grid.Column="0" Grid.Row="2" Height="35" Width="180" FontSize="20" Foreground="#000000" Background="#FF6100" BorderThickness="0" FontFamily="Times New Roman">
            <TextBlock>Все товары</TextBlock>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border 
    Background="{TemplateBinding Background}"
    BorderBrush="{TemplateBinding BorderBrush}"
    BorderThickness="{TemplateBinding BorderThickness}"
    CornerRadius="20">
                        <ContentPresenter 
        HorizontalAlignment="Center" 
        VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button Grid.Column="1"  Grid.Row="2" Height="35" Width="180" FontSize="20" Foreground="#000000" Background="#FF6100" BorderThickness="0" FontFamily="Times New Roman">
            <TextBlock>Добавить</TextBlock>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="20">
                        <ContentPresenter 
    HorizontalAlignment="Center" 
    VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>

</Window>

