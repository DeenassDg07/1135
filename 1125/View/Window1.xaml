<Window x:Class="_1125.View.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1125.View"
        mc:Ignorable="d"
        Title="Window1" Height="200" Width="700">
    <Grid>
        
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="125"/>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>


            <Image Source="{Binding ImageBitmap}" Grid.Column="0" Height="180" Width="125" Stretch="Uniform" />

                <StackPanel Grid.Column="1">
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Название:" FontSize="13" Foreground="#242527"/>
                        <TextBlock Width="320" Height="20" Text="{Binding Name}" />
                    </StackPanel>

                    <Border CornerRadius="10" BorderThickness="1" BorderBrush="#FFA4A4A4" Background="#FFA4A4A4">
                        <TextBlock Text="{Binding Description}" FontSize="13" Height="120" TextWrapping="Wrap"/>
                    </Border>

                    <StackPanel Orientation="Horizontal">
                        <Label Content="Осталось:" FontSize="13" Foreground="#FF6100"/>
                        <TextBlock Width="20" Height="17" HorizontalAlignment="Center" TextAlignment="Center"  Text="{Binding Availability}"/>
                        <Label Content="Шт" FontSize="13" Foreground="#FF6100"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" Width="175">
                    <Label Content="Цена:" FontSize="20" Foreground="#6EA7B5" Height="35"/>
                    <Border BorderBrush="#242527" BorderThickness="3" Height="35" Width="100">
                        <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Price}"/>
                    </Border>
                </StackPanel>

                <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
CommandParameter="{Binding}" IsEnabled="{Binding CanBaket}" Grid.Column="2" BorderThickness="0" Margin="10,10,10,10" VerticalAlignment="Bottom" Content="В корзину" Foreground="White" Background="#FF6100" FontSize="20" Width="150" Height="40">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
CornerRadius="20"  IsEnabled="{Binding CanBaket}"
Opacity="{Binding IsEnabled, RelativeSource={RelativeSource TemplatedParent} }">
                                <ContentPresenter 
HorizontalAlignment="Center" 
VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
       


    </Grid>
</Window>
